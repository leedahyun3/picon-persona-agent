services:
  - type: web
    name: picon-persona-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python template_server.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
