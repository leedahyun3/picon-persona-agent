services:
  - type: web
    name: picon-persona-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python root_cli.py serve-agent --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
